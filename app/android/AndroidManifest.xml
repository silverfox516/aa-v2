<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aauto.app"
    android:sharedUserId="android.uid.system"
    android:versionCode="12"
    android:versionName="0.3.0">

    <uses-feature android:name="android.hardware.usb.host" android:required="true" />
    <uses-permission android:name="android.permission.MANAGE_USB" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_PRIVILEGED" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NETWORK_STACK" />

    <application
        android:name=".AaApplication"
        android:label="Android Auto"
        android:persistent="true">

        <!-- Home screen: device list -->
        <activity
            android:name=".DeviceListActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- AA video display (launched by Service on session start).
             singleTask: re-using the existing instance keeps the live
             SurfaceView intact when activateSession() fires for a new
             session id. Without it, TaskStackBuilder creates a fresh
             instance and the prior Surface is destroyed mid-decode,
             which races with incoming video frames. -->
        <activity
            android:name=".AaDisplayActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <service
            android:name=".AaService"
            android:exported="false" />

    </application>
</manifest>
